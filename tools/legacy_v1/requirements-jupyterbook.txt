# Build dependencies for the Jupyter Book version of the site.
# Install into your existing conda env with:
#     pip install -r requirements-jupyterbook.txt
#
# Versions pinned to the youngest combination known to work together as
# of mid-2025; bump them as needed.

jupyter-book>=1.0.3
sphinx>=7.3,<8
myst-parser>=2.0
myst-nb>=1.1                # render chap*.ipynb pages
sphinx-book-theme>=1.1
sphinxcontrib-bibtex>=2.6
sphinxcontrib-mermaid>=0.9  # the mermaid block in docs/chap3.md
sphinx-proof>=0.2           # nicer theorem / proof admonitions
sphinx-design>=0.6          # tab panels, cards, etc.
linkify-it-py>=2.0          # auto-link URLs in MyST
